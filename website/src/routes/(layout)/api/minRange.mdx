import { Description, Property, Qwik, Title } from '~/components';
import { getFramework } from '~/utils';

<Title>minRange</Title>
<Description>
  Creates a validation functions that validates the range of a string, number or
  date.
</Description>

# minRange

Creates a validation functions that validates the range of a string<Qwik>, number or date</Qwik>.

```ts
const validate = minRange(requirement, error);
```

## Parameters

- `requirement` <Property {...properties.requirement} />
- `error` <Property type="string" />

## Return

- `validate` <Property {...properties[getFramework()]?.validate} />

export const properties = {
  requirement: {
    type: ['string', 'number', { type: 'custom', name: 'Date' }],
  },
  solid: {
    validate: {
      type: {
        type: 'function',
        params: [
          {
            name: 'value',
            type: {
              type: 'custom',
              name: 'MaybeValue',
              generics: [
                ['string', 'number', { type: 'custom', name: 'Date' }],
              ],
            },
          },
        ],
        return: 'string',
      },
    },
  },
  qwik: {
    validate: {
      type: {
        type: 'custom',
        name: 'QRL',
        generics: [
          {
            type: 'function',
            params: [
              {
                name: 'value',
                type: {
                  type: 'custom',
                  name: 'MaybeValue',
                  generics: [
                    ['string', 'number', { type: 'custom', name: 'Date' }],
                  ],
                },
              },
            ],
            return: 'string',
          },
        ],
      },
    },
  },
};
