import { Description, Property, Title } from '~/components';

<Title>ValidateForm</Title>
<Description>
  Type that defines the validation function of the form.
</Description>

# ValidateForm

Type that defines the validation function of the form.

## Definition

- `ValidateForm` <Property {...properties.ValidateForm} />

export const properties = {
  ValidateForm: {
    type: {
      type: 'function',
      params: [
        {
          name: 'values',
          type: {
            type: 'custom',
            name: 'PartialValues',
            generics: [
              { type: 'custom', name: 'FieldValue', href: '../FieldValue' },
            ],
          },
        },
      ],
      return: {
        type: 'custom',
        name: 'MaybePromise',
        generics: [
          { type: 'custom', name: 'FormErrors', href: '../FormErrors' },
        ],
      },
    },
  },
};
