import {
  Description,
  Property,
  Qwik,
  SolidPreactOrReact,
  Title,
} from '~/components';
import { isReact } from '~/contexts';

<Title>FormOptions</Title>
<Description>Type that defines the options of a form.</Description>

# FormOptions

Type that defines the options of a form.

## Definition

<SolidPreactOrReact block>

- `FormOptions` <Property {...properties.FormOptions} />
  - `initialValues` <Property {...properties.solidPreactOrReact.initialValues} />
  - `validate` <Property {...properties.solidPreactOrReact.validate} />
  - `validateOn` <Property {...properties[isReact() ? 'react' : 'solidQwikOrReact'].validateOn} />
  - `revalidateOn` <Property {...properties[isReact() ? 'react' : 'solidQwikOrReact'].revalidateOn} />

</SolidPreactOrReact>

<Qwik block>

- `FormOptions` <Property {...properties.FormOptions} />
  - `loader` <Property {...properties.qwik.loader} />
  - `action` <Property {...properties.qwik.action} />
  - `validate` <Property {...properties.qwik.validate} />
  - `validateOn` <Property {...properties.solidQwikOrReact.validateOn} />
  - `revalidateOn` <Property {...properties.solidQwikOrReact.revalidateOn} />

</Qwik>

### Explanation

With `validateOn` and `revalidateOn` you can define your validation strategy. `validateOn` defines the time of the first validation and `revalidateOn` defines when the validation is performed again. If you set `validateOn` to `'submit'`, you set the validation strategy at the form level. In this case `revalidateOn` is applied whenever the form has already been submitted once. For all other values it refers to a single field. In this case `revalidateOn` is applied if an error has already occurred.

export const properties = {
  FormOptions: {
    type: 'object',
  },
  solidPreactOrReact: {
    initialValues: {
      type: {
        type: 'Custom',
        name: 'PartialValues',
        href: '../PartialValues',
        generics: [
          {
            type: 'Custom',
            name: 'FieldValues',
            href: '../FieldValues',
          },
        ],
      },
    },
    validate: {
      type: {
        type: 'custom',
        name: 'Maybe',
        href: '../Maybe',
        generics: [
          {
            type: 'custom',
            name: 'ValidateForm',
            href: '../ValidateForm',
          },
        ],
      },
    },
  },
  solidQwikOrReact: {
    validateOn: {
      type: [
        { type: 'string', value: 'touched' },
        { type: 'string', value: 'input' },
        { type: 'string', value: 'change' },
        { type: 'string', value: 'blur' },
        { type: 'string', value: 'submit' },
      ],
      defaultValue: { type: 'string', value: 'submit' },
    },
    revalidateOn: {
      type: [
        { type: 'string', value: 'touched' },
        { type: 'string', value: 'input' },
        { type: 'string', value: 'change' },
        { type: 'string', value: 'blur' },
        { type: 'string', value: 'submit' },
      ],
      defaultValue: { type: 'string', value: 'input' },
    },
  },
  qwik: {
    loader: {
      type: {
        type: 'custom',
        name: 'Readonly',
        generics: [
          {
            type: 'Custom',
            name: 'Signal',
            generics: [
              {
                type: 'Custom',
                name: 'InitialValues',
                href: '../InitialValues',
                generics: [
                  {
                    type: 'Custom',
                    name: 'FieldValues',
                    href: '../FieldValues',
                  },
                ],
              },
            ],
          },
        ],
      },
    },
    action: {
      type: {
        type: 'custom',
        name: 'Maybe',
        href: '../Maybe',
        generics: [
          {
            type: 'Custom',
            name: 'ActionStore',
            generics: [
              {
                type: 'Custom',
                name: 'FormActionStore',
                href: '../FormActionStore',
              },
              {
                type: 'Custom',
                name: 'PartialValues',
                href: '../PartialValues',
                generics: [
                  {
                    type: 'Custom',
                    name: 'FieldValues',
                    href: '../FieldValues',
                  },
                ],
              },
              { type: 'boolean', value: true },
            ],
          },
        ],
      },
    },
    validate: {
      type: {
        type: 'custom',
        name: 'Maybe',
        href: '../Maybe',
        generics: [
          {
            type: 'custom',
            name: 'QRL',
            generics: [
              {
                type: 'custom',
                name: 'ValidateForm',
                href: '../ValidateForm',
              },
            ],
          },
        ],
      },
    },
  },
  react: {
    validateOn: {
      type: [
        { type: 'string', value: 'touched' },
        { type: 'string', value: 'change' },
        { type: 'string', value: 'blur' },
        { type: 'string', value: 'submit' },
      ],
      defaultValue: { type: 'string', value: 'submit' },
    },
    revalidateOn: {
      type: [
        { type: 'string', value: 'touched' },
        { type: 'string', value: 'change' },
        { type: 'string', value: 'blur' },
        { type: 'string', value: 'submit' },
      ],
      defaultValue: { type: 'string', value: 'change' },
    },
  },
};
