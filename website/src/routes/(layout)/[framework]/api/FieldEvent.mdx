import {
  Description,
  Property,
  Qwik,
  React,
  SolidOrPreact,
  Title,
} from '~/components';

<Title>FieldEvent</Title>
<Description>Type that defines the HTML element of a field.</Description>

# FieldEvent

Type that defines the HTML element of a field.

## Definition

<SolidOrPreact block>

- `FieldEvent` <Property {...properties.solidPreactOrReact.FieldEvent} />
  - `currentTarget` <Property {...properties.solidPreactOrReact.currentTarget} />

</SolidOrPreact>

<Qwik block>

- `FieldEvent` <Property {...properties.qwik.FieldEvent} />

</Qwik>

<React block>

- `FieldEvent` <Property {...properties.solidPreactOrReact.FieldEvent} />
  - `target` <Property {...properties.solidPreactOrReact.currentTarget} />

</React>

export const properties = {
  solidPreactOrReact: {
    FieldEvent: {
      type: {
        type: 'custom',
        name: 'Event',
      },
    },
    currentTarget: {
      type: {
        type: 'custom',
        name: 'FieldElement',
        href: '../FieldElement',
      },
    },
  },
  qwik: {
    FieldEvent: {
      type: [
        {
          type: 'custom',
          name: 'Event',
        },
        {
          type: 'custom',
          name: 'QwikChangeEvent',
          generics: [
            {
              type: 'custom',
              name: 'FieldElement',
              href: '../FieldElement',
            },
          ],
        },
        {
          type: 'custom',
          name: 'QwikFocusEvent',
          generics: [
            {
              type: 'custom',
              name: 'FieldElement',
              href: '../FieldElement',
            },
          ],
        },
      ],
    },
  },
};
