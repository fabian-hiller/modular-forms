import { Description, Property, Title } from '~/components';

<Title>required</Title>
<Description>
  Creates a validation function that checks the existence of an input.
</Description>

# required

Creates a validation function that checks the existence of an input.

```ts
const validate = required(error);
```

## Parameter

- `error` <Property type="string" />

## Return

- `validate` <Property {...properties.validate} />

export const properties = {
  validate: {
    type: {
      type: 'function',
      params: [
        {
          name: 'value',
          type: [
            { type: 'custom', name: 'ModularValue', href: 'modularvalue' },
            'undefined',
          ],
        },
      ],
      return: 'string',
    },
  },
};
