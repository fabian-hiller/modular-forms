import { Description, Property, Title } from '~/components';

<Title>url</Title>
<Description>Creates a validation functions that validates a URL.</Description>

# url

Creates a validation functions that validates a URL.

```ts
const validate = url(error);
```

## Parameter

- `error` <Property type="string" />

## Return

- `validate` <Property {...properties.validate} />

export const properties = {
  validate: {
    type: {
      type: 'function',
      params: [
        {
          name: 'value',
          type: ['string', 'null', 'undefined'],
        },
      ],
      return: 'string',
    },
  },
};
