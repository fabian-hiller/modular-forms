import { Description, Property, Title } from '~/components';
import { getFramework } from '~/utils';

<Title>ValidateFieldArray</Title>
<Description>
  Type that defines the validation function of a field array.
</Description>

# ValidateFieldArray

Type that defines the validation function of a field array.

## Definition

- `ValidateFieldArray` <Property {...properties[getFramework()]?.ValidateFieldArray} />

export const properties = {
  solid: {
    ValidateFieldArray: {
      type: {
        type: 'function',
        params: [
          {
            name: 'items',
            type: { type: 'array', item: 'number' },
          },
        ],
        return: [
          'string',
          { type: 'custom', name: 'Promise', generics: ['string'] },
        ],
      },
    },
  },
  qwik: {
    ValidateFieldArray: {
      type: {
        type: 'custom',
        name: 'QRL',
        generics: [
          {
            type: 'function',
            params: [
              {
                name: 'items',
                type: { type: 'array', item: 'number' },
              },
            ],
            return: [
              'string',
              { type: 'custom', name: 'Promise', generics: ['string'] },
            ],
          },
        ],
      },
    },
  },
};
