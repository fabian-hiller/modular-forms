import { Description, Property, Qwik, Title } from '~/components';
import { getFramework } from '~/utils';

<Title>minRange</Title>
<Description>
  Creates a validation functions that validates the range of a string or date.
</Description>

# minRange

Creates a validation functions that validates the range of a string<Qwik> or date</Qwik>.

```ts
const validate = minRange(requirement, error);
```

## Parameters

- `requirement` <Property {...properties[getFramework()]?.requirement} />
- `error` <Property type="string" />

## Return

- `validate` <Property {...properties[getFramework()]?.validate} />

export const properties = {
  solid: {
    requirement: { type: 'string' },
    validate: {
      type: {
        type: 'function',
        params: [
          {
            name: 'value',
            type: ['string', 'null', 'undefined'],
          },
        ],
        return: 'string',
      },
    },
  },
  qwik: {
    requirement: { type: ['string', { type: 'custom', name: 'Date' }] },
    validate: {
      type: {
        type: 'custom',
        name: 'QRL',
        generics: [
          {
            type: 'function',
            params: [
              {
                name: 'value',
                type: [
                  'string',
                  { type: 'custom', name: 'Date' },
                  'null',
                  'undefined',
                ],
              },
            ],
            return: 'string',
          },
        ],
      },
    },
  },
};
