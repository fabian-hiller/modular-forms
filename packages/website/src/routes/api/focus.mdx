import { Meta, Title } from 'solid-start';
import { Property } from '~/components';

<Title>focus | Modular Forms</Title>
<Meta name="description" content="Focuses the specified field of the form." />

# focus

Focuses the specified field of the form.

```ts
focus(form, name);
```

## Parameters

- `form` <Property {...properties.form} />
- `name` <Property type="string" />

export const properties = {
  form: {
    type: {
      type: 'custom',
      name: 'ModularForm',
      href: 'modularform',
    },
  },
};
