import { HttpStatusCode } from 'solid-start/server';
import { Description, Property, Qwik, Solid, Title } from '~/components';

<Title>MaybeFunction</Title>
<Description>
  Utility type that transforms a type into a maybe Function type.
</Description>

# MaybeFunction

<Qwik block>

Utility type that transforms a type into a maybe `Function` type.

## Generic

- `Value` <Property {...properties.Value} />

## Return

- `Type` <Property {...properties.Type} />

</Qwik>

<Solid block>

<HttpStatusCode code={404} />

> This page is exclusively for the Qwik library of Modular Forms.

</Solid>

export const properties = {
  Value: {
    type: 'any',
  },
  Type: {
    type: [
      { type: 'custom', name: 'Value' },
      { type: 'function', return: { type: 'custom', name: 'Value' } },
    ],
  },
};
