import { Description, Property, Title } from '~/components';

<Title>Response</Title>
<Description>Type that defines the response of a form.</Description>

# Response

Type that defines the response of a form.

## Definition

- `Response` <Property type="object" />
  - `status` <Property {...properties.status} />
  - `message` <Property {...properties.message} />

export const properties = {
  status: {
    type: [
      { type: 'string', value: 'info' },
      { type: 'string', value: 'error' },
      { type: 'string', value: 'success' },
      'undefined',
    ],
  },
  message: {
    type: ['string', 'undefined'],
  },
};
