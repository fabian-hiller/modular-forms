import {
  Description,
  Property,
  Qwik,
  SolidOrPreact,
  Title,
} from '~/components';

<Title>FieldEvent</Title>
<Description>Type that defines the HTML element of a field.</Description>

# FieldEvent

Type that defines the HTML element of a field.

## Definition

<SolidOrPreact block>

- `FieldEvent` <Property {...properties.default.FieldEvent} />
  - `currentTarget` <Property {...properties.default.currentTarget} />

</SolidOrPreact>

<Qwik block>

- `FieldEvent` <Property {...properties.qwik.FieldEvent} />

</Qwik>

export const properties = {
  default: {
    FieldEvent: {
      type: {
        type: 'custom',
        name: 'Event',
      },
    },
    currentTarget: {
      type: {
        type: 'custom',
        name: 'FieldElement',
        href: '../FieldElement',
      },
    },
  },
  qwik: {
    FieldEvent: {
      type: [
        {
          type: 'custom',
          name: 'Event',
        },
        {
          type: 'custom',
          name: 'QwikChangeEvent',
          generics: [
            {
              type: 'custom',
              name: 'FieldElement',
              href: '../FieldElement',
            },
          ],
        },
        {
          type: 'custom',
          name: 'QwikFocusEvent',
          generics: [
            {
              type: 'custom',
              name: 'FieldElement',
              href: '../FieldElement',
            },
          ],
        },
      ],
    },
  },
};
