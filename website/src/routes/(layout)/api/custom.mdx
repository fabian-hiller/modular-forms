import { Description, Property, Title } from '~/components';
import { getFramework } from '~/utils';

<Title>custom</Title>
<Description>Creates a custom validation function.</Description>

# custom

Creates a custom validation function.

```ts
const validate = custom(requirement, error);
```

## Parameters

- `requirement` <Property {...properties[getFramework()]?.requirement} />
- `error` <Property type="string" />

## Return

- `validate` <Property {...properties[getFramework()]?.validate} />

export const properties = {
  solid: {
    requirement: {
      type: {
        type: 'function',
        params: [
          {
            name: 'value',
            type: [
              { type: 'custom', name: 'FieldValue', href: '/api/FieldValue' },
              'undefined',
            ],
          },
        ],
        return: [
          'boolean',
          { type: 'custom', name: 'Promise', generics: ['boolean'] },
        ],
      },
    },
    validate: {
      type: {
        type: 'function',
        params: [
          {
            name: 'value',
            type: [
              { type: 'custom', name: 'FieldValue', href: '/api/FieldValue' },
              'undefined',
            ],
          },
        ],
        return: { type: 'custom', name: 'Promise', generics: ['string'] },
      },
    },
  },
  qwik: {
    requirement: {
      type: {
        type: 'custom',
        name: 'QRL',
        generics: [
          {
            type: 'function',
            params: [
              {
                name: 'value',
                type: [
                  {
                    type: 'custom',
                    name: 'FieldValue',
                    href: '/api/FieldValue',
                  },
                  'undefined',
                ],
              },
            ],
            return: [
              'boolean',
              { type: 'custom', name: 'Promise', generics: ['boolean'] },
            ],
          },
        ],
      },
    },
    validate: {
      type: {
        type: 'custom',
        name: 'QRL',
        generics: [
          {
            type: 'function',
            params: [
              {
                name: 'value',
                type: [
                  {
                    type: 'custom',
                    name: 'FieldValue',
                    href: '/api/FieldValue',
                  },
                  'undefined',
                ],
              },
            ],
            return: { type: 'custom', name: 'Promise', generics: ['string'] },
          },
        ],
      },
    },
  },
};
