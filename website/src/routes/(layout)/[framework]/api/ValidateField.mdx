import { Description, Property, Title } from '~/components';

<Title>ValidateField</Title>
<Description>Type that defines the validation function of a field.</Description>

# ValidateField

Type that defines the validation function of a field.

## Definition

- `ValidateField` <Property {...properties.ValidateField} />

export const properties = {
  ValidateField: {
    type: {
      type: 'function',
      params: [
        {
          name: 'value',
          type: {
            type: 'custom',
            name: 'Maybe',
            generics: [
              { type: 'custom', name: 'FieldValue', href: '../FieldValue' },
            ],
          },
        },
      ],
      return: { type: 'custom', name: 'MaybePromise', generics: ['string'] },
    },
  },
};
