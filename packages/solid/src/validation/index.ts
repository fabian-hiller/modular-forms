export {
  custom,
  email,
  maxLength,
  maxRange,
  maxSize,
  maxTotalSize,
  mimeType,
  minLength,
  minRange,
  minSize,
  minTotalSize,
  pattern,
  required,
  url,
  value,
} from '@modular-forms/core';
