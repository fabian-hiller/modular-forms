import { Description, Property, Title } from '~/components';

<Title>focus</Title>
<Description>Focuses the specified field of the form.</Description>

# focus

Focuses the specified field of the form.

```ts
focus(form, name);
```

## Parameters

- `form` <Property {...properties.form} />
- `name` <Property type="string" />

export const properties = {
  form: {
    type: {
      type: 'custom',
      name: 'FormStore',
      href: '../FormStore',
    },
  },
};
