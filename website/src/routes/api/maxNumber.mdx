import { Description, Property, Title } from '~/components';

<Title>maxNumber</Title>
<Description>
  Creates a validation functions that validates a number.
</Description>

# maxNumber

Creates a validation functions that validates a number.

```ts
const validate = maxNumber(requirement, error);
```

## Parameters

- `requirement` <Property type="number" />
- `error` <Property type="string" />

## Return

- `validate` <Property {...properties.validate} />

export const properties = {
  validate: {
    type: {
      type: 'function',
      params: [
        {
          name: 'value',
          type: ['number', 'null', 'undefined'],
        },
      ],
      return: 'string',
    },
  },
};
