import { Meta, Title } from 'solid-start';
import { Property } from '~/components';

<Title>email | Modular Forms</Title>
<Meta
  name="description"
  content="Creates a validation functions that validates a email."
/>

# email

Creates a validation functions that validates a email.

```ts
const validate = email(error);
```

## Parameter

- `error` <Property type="string" />

## Return

- `validate` <Property {...properties.validate} />

export const properties = {
  validate: {
    type: {
      type: 'function',
      params: [
        {
          name: 'value',
          type: ['string', 'null', 'undefined'],
        },
      ],
      return: 'string',
    },
  },
};
