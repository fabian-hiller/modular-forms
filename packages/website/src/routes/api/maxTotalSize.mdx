import { Meta, Title } from 'solid-start';
import { Property } from '~/components';

<Title>maxTotalSize | Modular Forms</Title>
<Meta
  name="description"
  content="Creates a validation functions that validates total file size of a file list."
/>

# maxTotalSize

Creates a validation functions that validates total file size of a file list.

```ts
const validate = maxTotalSize(requirement, error);
```

## Parameters

- `requirement` <Property type="number" />
- `error` <Property type="string" />

## Return

- `validate` <Property {...properties.validate} />

export const properties = {
  validate: {
    type: {
      type: 'function',
      params: [
        {
          name: 'value',
          type: [{ type: 'custom', name: 'FileList' }, 'null', 'undefined'],
        },
      ],
      return: 'string',
    },
  },
};
