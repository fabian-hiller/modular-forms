import { A } from 'solid-start';
import { Description, Property, Title } from '~/components';
import { isQwik } from '~/contexts';

<Title>FieldValue</Title>
<Description>Type that defines the value of a field.</Description>

# FieldValue

Type that defines the value of a field.

## Definition

- `FieldValue` <Property {...properties[isQwik() ? 'qwik' : 'solidPreactOrReact'].FieldValue} />

> Make sure to use the correct input fields with the correct setting for the respective values. You can find instructions <A href="../../guides/special-inputs">here</A>.

export const properties = {
  solidPreactOrReact: {
    FieldValue: {
      type: [
        'string',
        { type: 'array', item: 'string' },
        'number',
        'boolean',
        'null',
        'undefined',
        { type: 'custom', name: 'File' },
        {
          type: 'array',
          item: {
            type: 'custom',
            name: 'File',
          },
        },
        { type: 'custom', name: 'Date' },
      ],
    },
  },
  qwik: {
    FieldValue: {
      type: [
        'string',
        { type: 'array', item: 'string' },
        'number',
        'boolean',
        'null',
        'undefined',
        {
          type: 'custom',
          name: 'NoSerialize',
          generics: [{ type: 'custom', name: 'Blob' }],
        },
        {
          type: 'custom',
          name: 'NoSerialize',
          generics: [{ type: 'custom', name: 'File' }],
        },
        {
          type: 'array',
          item: {
            type: 'custom',
            name: 'NoSerialize',
            generics: [{ type: 'custom', name: 'Blob' }],
          },
        },
        {
          type: 'array',
          item: {
            type: 'custom',
            name: 'NoSerialize',
            generics: [{ type: 'custom', name: 'File' }],
          },
        },
        { type: 'custom', name: 'Date' },
      ],
    },
  },
};
