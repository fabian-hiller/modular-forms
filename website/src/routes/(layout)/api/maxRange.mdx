import { Description, Property, Title } from '~/components';

<Title>maxRange</Title>
<Description>
  Creates a validation functions that validates the range of a string.
</Description>

# maxRange

Creates a validation functions that validates the range of a string.

```ts
const validate = maxRange(requirement, error);
```

## Parameters

- `requirement` <Property type="string" />
- `error` <Property type="string" />

## Return

- `validate` <Property {...properties.validate} />

export const properties = {
  validate: {
    type: {
      type: 'function',
      params: [
        {
          name: 'value',
          type: ['string', 'null', 'undefined'],
        },
      ],
      return: 'string',
    },
  },
};
