import { Meta, Title } from 'solid-start';
import { Property } from '~/components';

<Title>ModularValue | Modular Forms</Title>
<Meta name="description" content="Type that defines the value of a field." />

# ModularValue

Type that defines the value of a field.

## Definition

- `ModularValue` <Property {...properties.ModularValue} />

> Make sure to use the correct input fields with the correct setting for the respective values. You can find instructions [here](/guides/special-inputs).

export const properties = {
  ModularValue: {
    type: [
      'string',
      'number',
      'boolean',
      'null',
      'undefined',
      { type: 'array', item: 'string' },
      {
        type: 'custom',
        name: 'File',
      },
      {
        type: 'custom',
        name: 'FileList',
      },
    ],
  },
};
