import { HttpStatusCode } from 'solid-start/server';
import {
  Description,
  Property,
  Qwik,
  SolidPreactOrReact,
  Title,
} from '~/components';

<Title>valiField</Title>
<Description>
  Creates a validation functions that parses the Valibot schema of a field.
</Description>

# valiField

<SolidPreactOrReact block>

Creates a validation functions that parses the Valibot schema of a field.

```ts
const validate = valiField(schema);
```

## Parameters

- `schema` <Property {...properties.schema} />

## Return

- `validate` <Property {...properties.validate} />

</SolidPreactOrReact>

<Qwik block>

<HttpStatusCode code={404} />

> This page is exclusively for the SolidJS, Preact and React library of Modular Forms.

</Qwik>

export const properties = {
  schema: {
    type: [
      {
        type: 'custom',
        name: 'BaseSchema',
        generics: [
          {
            type: 'custom',
            name: 'FieldValue',
            href: '../FieldValue',
          },
          'any',
        ],
      },
      {
        type: 'custom',
        name: 'BaseSchemaAsync',
        generics: [
          {
            type: 'custom',
            name: 'FieldValue',
            href: '../FieldValue',
          },
          'any',
        ],
      },
    ],
  },
  validate: {
    type: {
      type: 'function',
      params: [
        {
          name: 'value',
          type: { type: 'custom', name: 'FieldValue', href: '../FieldValue' },
        },
      ],
      return: 'string',
    },
  },
};
