import { Meta, Title } from 'solid-start';
import { Property } from '~/components';

<Title>clearResponse | Modular Forms</Title>
<Meta name="description" content="Clears the response of the form." />

# clearResponse

Clears the response of the form.

```ts
clearResponse(form);
```

## Parameters

- `form` <Property {...properties.form} />

export const properties = {
  form: {
    type: {
      type: 'custom',
      name: 'ModularForm',
      href: 'modularform',
    },
  },
};
