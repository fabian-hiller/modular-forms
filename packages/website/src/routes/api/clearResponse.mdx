import { Description, Property, Title } from '~/components';

<Title>clearResponse</Title>
<Description>Clears the response of the form.</Description>

# clearResponse

Clears the response of the form.

```ts
clearResponse(form);
```

## Parameters

- `form` <Property {...properties.form} />

export const properties = {
  form: {
    type: {
      type: 'custom',
      name: 'ModularForm',
      href: '/api/ModularForm',
    },
  },
};
